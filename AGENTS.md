# AGENTS.md — tipsterbyte-fx-v2

Proyecto de aprendizaje/portafolio Java para proceso de selección. Plataforma de pronósticos de fútbol que consume 4 APIs externas (football-data.org, API-Football, The Odds API, SharpAPI) con arquitectura limpia + DDD.

## Estado actual — CRÍTICO

- El repo está en **FASE 12 completada (Redis cache-aside) + FASE T1-b completada (API de consulta + CORS + estandarización de respuestas)**. FASE 8 (PostgreSQL + JPA): modelo de dominio completo en `domain.model` (aggregates `Liga/Partido/Pronostico/Suscripcion` con BR-001..008, + `Pais`, `FuenteExtraccion`, `DetalleFuenteExtraccion`, + `Usuario` de auth desde FASE 11), eventos en `domain.event`, factory methods `reconstruir(...)`. Capa application con **13 casos de uso (CU-01..13)** en `application.usecase` (CU-12 `RegistrarUsuarioUseCase`, CU-13 `AutenticarUsuarioUseCase`), puertos (`ProveedorPosiciones/Calendario/Cuotas` + repositorios + `PasswordHasher`/`TokenEmisor`/`UsuarioRepository` + **`CacheLecturas`/`CacheClaves` desde FASE 12**) en `application.port`, DTOs en `application.dto`. Capa interfaces con 8 controllers (`LigaController`, `PartidoController`, `PronosticoController`, `SuscripcionController`, `FuenteExtraccionController`, `AuthController`, `RolController`) y `GlobalExceptionHandler` (DomainException → 422, validación → 400). Capa infrastructure con 11 entidades JPA, 7 repositorios Spring Data y 7 adapters JPA (`infrastructure.adapter`), **más 3 adapters HTTP de fuentes**: `FlashscorePosicionesAdapter` (#3 → ProveedorPosiciones), `SoccerwayCalendarioAdapter` (#4 → ProveedorCalendario), `WplayCuotasAdapter` (#2 → ProveedorCuotas), y **FASE 12 cache**: puerto `CacheLecturas` implementado por `RedisCacheAdapter` (StringRedisTemplate + TTL, `app.cache.enabled=true`) o `NoOpCacheLecturas` (`=false`), con 3 decoradores cache-aside `@Primary` `ProveedorPosiciones/Calendario/CuotasCacheable` que envuelven los adapters reales (Jackson 3 ObjectMapper, `TypeReference` de `tools.jackson.core.type`); la invalidación ocurre en CU-01/02/03 (eliminan la clave antes de consultar). FASE 9 dockeriza el PostgreSQL de dev (`docker-compose.yml`: `postgres:17` en `:5434`, base `tipsterbytefxv2_dev`, volumen + red + healthcheck) y el datasource por defecto apunta a ese contenedor; **FASE 12 añade `redis:7-alpine` en `:6380`** (`tipsterbytefxv2-redis`, volumen + red + healthcheck). FASE 11 añade Spring Security + JWT (stateless, roles `CLIENTE`/`TIPSTER`/`SUPERADMIN`, BCrypt, registro/login en `/api/v1/auth/**`, token firmado HS256 con `app.jwt.secret`/`app.jwt.expiration-ms`). **`src/test/resources/application.properties` replica el principal con `app.cache.enabled=false`** (tests de contexto sin Redis; `RedisCacheAdapterTest` usa Testcontainers `GenericContainer redis:7-alpine` — no existe `testcontainers-redis` en Testcontainers 2.0.5). FASE T1-b añade **endpoints GET de consulta** para el frontend Angular: listado/detalle/posiciones de ligas, partidos por liga/fecha/próximos, cuotas por partido, suscripciones por cliente; **CORS** configurado para `http://localhost:4200`; y **estandarización de respuestas**: `ApiErrorAuthenticationEntryPoint` (401) y `ApiErrorAccessDeniedHandler` (403) devuelven `ApiError` en JSON, alineados con `GlobalExceptionHandler`. **426 tests en verde** (incluyen FASE T1-b, CU-14+maxLigasPorPais, FASE 12.5/CU-15 y el refactor Torneos/Temporadas). El wiring REST se activa con `app.api.rest.enabled=true` (por defecto): los use cases se registran como beans en `UseCaseConfig` (son POJOs sin anotaciones Spring).
- **FASE CU-14 completada (países de interés) + maxLigasPorPais**: entity `PaisInteres` (`domain.model`) ahora con `maxLigasPorPais` (Integer nullable, `null` = sin límite, `>= 1` si viene → 400 si no), tabla `paises_interes` con columna `max_ligas_por_pais`, endpoints `GET/POST/PUT/DELETE /api/v1/paises-interes` + `GET /api/v1/paises/disponibles` (roles SUPERADMIN/TIPSTER). El PUT recibe la lista completa `[{isoAlpha2, nombre, maxLigasPorPais}]` → 204; GET/POST devuelven el campo en el shape `{isoAlpha2, nombre, prioridad, maxLigasPorPais}`. CU-10 procesa primero los países de interés por prioridad y aplica su límite **en la fuente #5 (param limit) Y localmente** (.limit() como red de seguridad); los países sin preferencia se extraen sin tope y nunca se omiten. Contrato frontend: `docs/frontend/comunicado-paises-interes.md` (sección 0).
- **FASE 12.5 — Scheduling/Async COMPLETADA**: entity `TareaProgramada` + `TareaLog`, CU-15 `GestionarTareasProgramasUseCase`, `CatalogoScheduler` (dispatcher `@Scheduled` + anti-solapamiento), endpoints `/api/v1/tareas-programadas/**`. Verificado en código con tests. FASE 15 queda para scheduling avanzado (ShedLock).
- **Refactor Torneos/Temporadas COMPLETADO (Bridge Fix + fase dedicada)**: `Temporada` es Entity (id UUID, ligaId, nombre, semestre opcional 1|2, anioInicio/anioFin, `EstadoTemporada` PLANIFICADA/ACTIVA/FINALIZADA) y es el **centro del modelo deportivo**:
  - `Partido.temporadaId` y `DetalleFuenteExtraccion.temporadaId` (FKs a `temporadas.id`; las consultas "por liga" resuelven vía JOIN interno, adapters de fuentes intactos salvo Wplay que resuelve por temporada del partido).
  - `Temporada` compone `equipos` y `posiciones` en el dominio (un descendido en 2024 no está en la tabla 2025); `Liga` delega en su *temporada vigente* (activa o primera registrada) manteniendo la API `liga.equipos()/posiciones()/actualizarPosiciones()` para no tocar CU-01/controllers.
  - `equipos.temporada_id` y `posiciones_tabla.temporada_id` (fuera `liga_id` legado).
  - `ligas.pais_id` FK real a `paises.id` (CU-10 lo llena al poblar; el nombre `pais` queda denormalizado para display; query derivada renombrada a `findByEstadoAndPaisNombreIgnoreCase`).
  - CU-10 crea ligas de catálogo con `pais_id` + temporada PLANIFICADA derivada del campo `anio` (nombre = "AAAA/AAAA"); CU-04/CU-11 asocian URLs a la temporada vigente; nuevo método de puerto `buscarPorTemporadaYTipo`.
  - Esquema versionado con **Flyway (H-05)**: `db/migration/V1__baseline.sql` generado desde entidades; `ddl-auto=validate`; Testcontainers ejecuta las migraciones reales. Los scripts manuales históricos viven en `docs/migration/` solo como referencia.
  - **Fuente #6 (poblamiento de equipos, FASE T2/HU-11)**: `ProveedorEquiposPorLiga` + `SoccerwayEquiposAdapter` + cache Redis (`CacheClaves.equipos`, TTL largo). CU-10 puebla la plantilla (nombre + escudo) de la temporada vigente SOLO para ligas de países de interés; matching por nombre normalizado (`NormalizadorNombresEquipos` en domain.service — adoptado también por CU-01/CU-02); #6 nunca elimina; tolerancia a fallos por liga. `Equipo.logoUrl` → columna `equipos.logo_url`.
  - Pendiente fase futura: endpoints dedicados `/temporadas/**` y `/ligas/{id}/equipos`, transición PLANIFICADA→ACTIVA de temporadas (hoy nadie activa temporadas), matching fuzzy de equipos (FASE 17).
- **FASE HU-12 — Poblamiento granular COMPLETADA (hibrido)**: desglose del recorrido completo para el panel Geografía — `POST /api/v1/catalogo/poblar-paises` (CU-17 `SincronizarPaisesUseCase`, 200 sync, ~176 filas #1, void + `GET /estado`) y `POST /api/v1/catalogo/poblar-ligas/{isoAlpha2}` (CU-18 `SincronizarLigasPorPaisUseCase` + wrapper async `SincronizarLigasPorPaisAsyncUseCase`, 202 + polling `GET /activar/{executionId}` reutilizando `TareaLog` RUNNING/SUCCESS/ERROR; anti-solapamiento por `isoAlpha2` → 409 solo mismo país, permite CO+AR en paralelo). Respeta `maxLigasPorPais` (CU-14) + puebla equipos #6 si país de interés; `LigaRepository.buscarPorPais`. Contrato frontend: `docs/frontend/comunicado-poblamiento-granular.md` (hibrido 200/202, shapes `PoblamientoIniciadoResponse`).
- Avanzamos **una fase a la vez** (ver `docs/PROYECTO-PLAN.md`), con aprobación explícita del usuario entre fases. **NUNCA** ejecutar fases adelantadas ni "construir el proyecto completo".
- **Decisión FASE 9 (aislamiento de BDs)**: la BD dockerizada de este proyecto (`:5434`, `tipsterbytefxv2_dev`) es **exclusiva**; NO se usa ni modifica la BD compartida del proyecto Python (`db_pg_tipsterbyte_fx_dev` en `localhost:5433`). Testcontainers (tests) usa su propio contenedor, independiente de ambos.
- **Documento de referencia: `docs/architecture/fuentes-externas.md`** — registra las **5 fuentes reales** expuestas por el proyecto Python (`http://127.0.0.1:8001`): `#1 ext-soccerway-countries` (países), `#5 ext-soccerway-leagues-by-country` (ligas por país, params `country_name`/`limit`), `#3 ext-position-table-by-league-stable` (posiciones + últimos 5 resultados por equipo, param `path_to_scrape`), `#4 ext-calendar-league-by-league-v2` (calendario + estadísticas, param `path_to_scrape`) y `#2 ext-next-matches-wplay-by-league` (cuotas Wplay incl. doble oportunidad, param `path_to_scrape`). Orden de extracción: países (#1) → ligas por país (#5) → posiciones (#3)/calendario (#4)/cuotas (#2) por liga. Mapeo: #1/#5 → CU-10 (catálogo), #2 → ProveedorCuotas, #3 → ProveedorPosiciones, #4 → ProveedorCalendario. **Los 5 esquemas están confirmados** (JSON real en el doc).
- **Decisiones FASE 8.5**: catálogo de fuentes vía CU-11 `GestionarFuenteExtraccionUseCase` (registrar/listar fuentes, asociar URL por liga, listar detalles); CU-04 `ActivarLigaUseCase` reescrito con `ActivarLigaComando` (recibe `urlPosiciones`/`urlCalendario`/`urlCuotas` → crea `DetalleFuenteExtraccion` por tipo y activa con BR-001); `Cuota` ganó `mercado` (canónico `Cuota(Mercado, BigDecimal)`; `Cuota(BigDecimal)` → UNO_X_DOS); `PosicionTabla` ganó `ultimosResultados` (clave 1 = más reciente, G/E/P) persistido como VARCHAR "G,E,P,G,G"; equipos se matchean por **nombre exacto** (fuzzy difiere a FASE 17); `SoccerwayCalendarioAdapter` **solo crea partidos**; tablas JPA propias `fuentes_extraccion`/`detalle_fuentes_extraccion` (unique (liga_id, tipo)); **no se reutilizan** las tablas `fuente_extraccion`/`detalle_fuente_extraccion` del esquema Python.
- **Decisión roles (unificación a `CLIENTE`/`TIPSTER`/`SUPERADMIN`)**: los roles son un **enum del dominio** (`domain.model.Rol`), única fuente de verdad, persistido por usuario en la columna `usuarios.rol` (auth FASE 11). El catálogo de roles se expone por `GET /api/v1/roles` (`RolController` + `RolResponse`, endpoint público para el formulario de registro del frontend). **No existe tabla maestra de roles ni seeder**: se eliminó el experimento `Role`/`RoleName`/`User`/`RoleRepository` (código muerto que siembra una tabla que nada lee y que chocaba con constraints viejos); las migrations de datos maestros formales llegan con Flyway en FASE 19/20. `SecurityConfig` usa `hasRole("SUPERADMIN"/"TIPSTER"/"CLIENTE")`.
- **Gotcha enums + constraints CHECK en BD dev (ADR-008)**: al cambiar un enum de dominio (ej. `TipoFuente`, `Rol`), la BD dev conserva el CHECK viejo (los INSERT con el valor nuevo fallan con **500 silencioso**; la causa real solo se ve en el stack trace de los logs de `bootRun`, nunca en la respuesta HTTP). Histórico: ocurrió con `ddl-auto=update` (`usuarios_rol_check`) y VOLVIÓ a ocurrir post-Flyway con `'EQUIPOS'` (fuente #6): `baseline-on-migrate=true` marcó V1 como aplicada **sin reconciliar** el esquema legacy pre-Flyway, dejando 3 CHECKs sin `'EQUIPOS'` (`fuentes_extraccion_tipo_check`, `detalle_fuentes_extraccion_tipo_check`, `tareas_programadas_tipo_fuente_check`) — corregido con migración **V6**. Reglas: (1) todo cambio de esquema va como migración `V(n+1)__*.sql`, **NUNCA ALTER manual** fuera de Flyway; (2) al añadir un valor de enum, auditar TODOS los CHECK constraints que lo referencien (skill `migrations-ddl-flyway-auto`, Gotcha 3); (3) para probar extracciones end-to-end (#2 cuotas/#3 posiciones/#4 calendario/#6 equipos) usar el runbook `docs/runbook-extraccion-fuentes.md` en vez de re-descubrir el flujo login→POST→polling. Detalle completo en `docs/architecture/arquitectura-objetivo.md` (ADR-008).

## Configuración de OpenCode (FASE 1)

- `.opencode/rules/` — 5 reglas siempre activas (architecture, java, documentation, naming, testing), registradas en `opencode.json` vía `instructions`.
- `.opencode/skills/` — skills activas: java-engineering, spring-boot-engineering, clean-architecture, ddd-domain-modeling, java-documentation, architecture-review, diagram-design (estilo visual), diagramas-interactivos (capas de interacción, OBLIGATORIA en todo diagrama). Más adelante: docker-engineering, persistence-jpa, testing-java, spring-security, redis-engineering, messaging-rabbitmq, spring-webflux, observability, aws-engineering.
- Reglas y skills se cargan al reiniciar OpenCode; no hay hot-reload.

## Fuentes de verdad (leer antes de tocar nada)

- `docs/PROYECTO-PLAN.md` — las 22 fases + FASE 12.5 (Scheduling/Async) y el orden de trabajo.
- `docs/project-definition.md` — nombre, problema, usuarios, funcionalidades.
- `docs/domain/modelo-dominio.md` — DDD: aggregates (`Liga`, `Partido`, `Pronostico`, `Suscripcion`), VOs, 8 reglas de negocio (BR-001..008), eventos.
- `docs/use-cases/casos-de-uso.md` — casos de uso (CU-01..14) con flujos y puertos.
- `docs/use-cases/historias-de-usuario.md` — 9 HU con trazabilidad HU → CU → puertos.
- `docs/architecture/arquitectura-objetivo.md` — capas, Dependency Rule, ports/adapters, 4 ADRs.
- `docs/frontend/comunicado-paises-interes.md` — contrato REST de CU-14 (shapes, status codes, semántica de prioridad) para el equipo frontend.
- `docs/frontend/comunicado-poblamiento-granular.md` — contrato REST de HU-12 (hibrido 200/202, poblar-paises + poblar-ligas/{iso}).

## Convenciones obligatorias

- **Documentación en español** (docs, comentarios, commits).
- Todo artefacto codificado debe documentar: `[QUÉ]`, `[POR QUÉ]`, `[ALTERNATIVAS]` y `[RELACIONES]` (con qué casos de uso/puertos/adapters se conecta). Estándar definido en FASE 1 (`.opencode/rules/documentation.md`).
- **Ports/adapters desde el inicio**: las 4 APIs se modelan como adapters de puertos de dominio (`ProveedorPosiciones`, `ProveedorCalendario`, `ProveedorCuotas`), nunca se acoplan al dominio.
- Comandos en **Git Bash** (Windows), no cmd/PowerShell (regla heredada del proyecto previo).

## Convención de diagramas interactivos (OBLIGATORIA)

- **Todo diagrama que se genere** (modelo de dominio, arquitectura, ERD de FASE 8, flujos de casos de uso, etc.) debe permitir que **sus elementos principales sean clicables**: clases/artefactos y relaciones.
- Al hacer **click** en un elemento se abre un **dialog modal** que ilustra esa parte del diagrama: `QUÉ` hace, `RELACIÓN` (para conectores: el atributo exacto, ej: `Pronostico.partidoId → Partido.id`; para clases: con qué se relaciona y por qué atributo), `REGLAS` (BR-xx), `MÉTODOS` y `EVENTOS`.
- El hover **no oculta nada**: solo indica clicabilidad (cursor pointer, borde más grueso, subrayado de título). El detalle vive detrás del click, no en tooltips.
- El modal **no debe producir salto de scroll**: al bloquear el scroll de fondo, compensar el ancho de la barra con `padding-right` (medido antes de ocultarla) para que el lienzo no se desplace horizontalmente al abrir/cerrar.
- El lienzo permite **scroll horizontal** cuando el diagrama no cabe: el SVG nunca se comprime por debajo del ancho de su `viewBox` (ej: `min-width:1320px` en un `div.lienzo` con `overflow-x:auto`) para que los textos de las relaciones no se solapen.
- El lienzo muestra la vista estructural (densidad baja); el detalle se explica en el modal.
- Referencia de implementación: skill `.opencode/skills/diagramas-interactivos/SKILL.md` y ejemplo `diagrams/modelo-dominio.html`.
- Se usa junto al skill `diagram-design` (estilo visual) para los diagramas del proyecto.

## Entorno

- Directorio de trabajo: `ecosistema_java` (este repo). Proyecto Spring Boot compilando en la raíz (`build.gradle`, `src/`).
- Referencia de estudio (NO se modifica): `../contexto_java/pronosticos-futbol` (proyecto Java hexagonal previo con `.clinerules`) y `../../contextos_gpt/tipsterByte_fx` (backend Python original).
- Build: `./gradlew build` (Git Bash). `./gradlew bootRun` levanta la app en `localhost:8080`.